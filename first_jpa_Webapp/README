After building this webapp with maven, the run_it.sh will get the webapp running. 
Once the webapp loads, open a browser to localhost:8080 

This webapp is running using Spring Boot and Spring Data.
It's configured to connect to a local Postgres db and both accesses and persists
data to the db.


